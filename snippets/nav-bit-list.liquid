{% comment %} Accepts bit_list, a section setting {% endcomment %}

<p class="mx-auto pb-5">Popular Router Bits</p>
<div class="flex ">
  {% for item in section.settings.bit_list %}
    <a href="{{ item.url }}">
      <div class="flex flex-col mr-12 max-w-[225px]">
        <img
          class="w-full h-auto object-cover"
          src="{{ item.featured_image | image_url }}"
          width="100%"
          height="100%"
        >
        <p class="text-center font-bold text-sm pt-4">{{ item.title }}</p>
      </div>
    </a>
  {% endfor %}
</div>
